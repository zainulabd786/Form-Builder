

<{{wrapperTag}} {{{wrapperAttributes}}}>
	<label {{{label_attributes}}}>{{label}} <a href="#" data-toggle="tooltip" data-placement="bottom" title="{{tooltip}}"><i class="fas fa-question-circle"></i></a></label></label>
	<{{inputTag}} {{{inputAttributes}}} >
		{{#choices}} <option value="{{.}}">{{.}}</option> {{/choices}}
	</{{inputTag}}>
	
</{{wrapperTag}}>
